<?php require_once './app/core/zinc_cli/cli_loader.php';

  // Execute all valid commands.
  // Param $env is from the cli_loader file, imported above.
  // Param $argv is the system arguments variable.
  
  \ZincPHP\CLI\execute( $env, $argv );
